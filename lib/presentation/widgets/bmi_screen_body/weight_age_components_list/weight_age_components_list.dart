import 'package:flutter/material.dart';

class WeightAgeComponentsList extends StatelessWidget {
  const WeightAgeComponentsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      mainAxisAlignment: MainAxisAlignment.spaceBetween,
      children: [
        
      ],
    );
  }
}